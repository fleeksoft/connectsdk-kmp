product:
  type: lib
  platforms: [ android, jvm, iosArm64, iosSimulatorArm64, iosX64 ]

dependencies:
  - $compose.foundation: exported
  - $compose.material3: exported
  - ../../connectcore: exported
  - $libs.compose.material.icons.extended

dependencies@android:
  - $libs.androidx.activity.compose: exported
  - $libs.androidx.appcompat: exported

dependencies@jvm:
  - $compose.desktop.currentOs

settings:
  compose: enabled
  kotlin:
    version: 2.3.0